#ifndef PROFILE_SELECTION_H
#define PROFILE_SELECTION_H

#include "settings.h"
#include "state.h"

void profile_selection(
    const struct Settings* settings,
    struct SharedState* state);

#endif

